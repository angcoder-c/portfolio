---
import TerminalWindow from './TerminalWindow.astro';
import GalleryCarousel from './GalleryCarousel';
import type { GalleryItem } from '../data/types';

interface Props {
  images: GalleryItem[];
  command?: string;
}

const { images, command = '$ ls ./gallery' } = Astro.props;
---

<section id="gallery">
  <TerminalWindow
    windowId="0xGAL_IMG"
    class="border-secondary/35 bg-[linear-gradient(225deg,rgba(0,229,255,0.05)_0%,transparent_50%),var(--color-surface)] shadow-[inset_0_0_40px_rgba(0,229,255,0.03)]"
  >
    <p class="mb-6 font-bold text-primary" data-cmd>{command}</p>
    <GalleryCarousel client:load images={images} />
  </TerminalWindow>
</section>
